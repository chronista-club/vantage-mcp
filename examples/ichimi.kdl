// Ichimi プロセス設定ファイル (KDL形式)

meta {
    version "1.0.0"
    // schemaとcreated_at、updated_atは省略（None）
}

// シンプルなプロセス定義
process "simple-server" {
    command "python"
    args "-m" "http.server" "8080"
    cwd "/tmp"
    auto_start #false
}

