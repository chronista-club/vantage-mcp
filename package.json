{
  "name": "ichimi-graceful-shutdown-tests",
  "version": "1.0.0",
  "description": "Comprehensive test suite for Ichimi Server graceful shutdown functionality",
  "type": "module",
  "scripts": {
    "test": "bun run test:graceful-shutdown",
    "test:graceful-shutdown": "bun run src/tests/graceful-shutdown-test.ts",
    "test:watch": "bun run --watch src/tests/graceful-shutdown-test.ts",
    "lint": "bun run --bun tsc --noEmit",
    "clean": "rm -rf dist coverage .nyc_output"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "ichimi",
    "process-management",
    "graceful-shutdown",
    "testing",
    "bun",
    "typescript"
  ],
  "author": "Ichimi Server Test Suite",
  "license": "MIT"
}